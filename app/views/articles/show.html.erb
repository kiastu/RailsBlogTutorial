<p>
	<strong>Title:</strong>
	<%= @article.title %>
</p>

<p>
	<strong>Text:</strong>
	<%= @article.text %>
</p>
<h1>Comments</h1>
<!-- Rails knows how to find the partial for _comment.html.erb in the views/comments/_comment.html.erb -->
<%= render @article.comments %>

<h2>Add a Comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Back', articles_path %>